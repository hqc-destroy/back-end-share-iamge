module.exports = {
    ROOT_API: {
        USER: '/api/users', // /api/user -> /api/users
    },
    USER: {
        CREATE_USER: '/', // /create-user -> /  note: Create a new user (method: post)
        LOGIN: '/login'
    }
}